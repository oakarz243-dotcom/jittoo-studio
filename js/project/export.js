export function exportCanvas(editor,format='png'){const mime=format==='jpg'?'image/jpeg':format==='webp'?'image/webp':'image/png';const ext=format==='jpg'?'jpg':format;editor.render();editor.canvas.toBlob(blob=>{if(!blob)return;const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`jittoo-export.${ext}`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)},mime,.92)}